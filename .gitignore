# Node modules
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml
package-lock.json

# Environment files (DO NOT COMMIT SECRETS)
.env
.env.*
!.env.example

# Temporary local secrets file — delete after migrating to Secret Manager
env_variables.yaml
env_variables.example.yaml

# One-time secret migration helper (contains no secrets, but not needed in repo)
create-secrets.sh

# Google Cloud / service account credentials
credentials-elysian-api.json
*.service-account.json
credentials.json
src/config/gcs-key.json

# Build output (if you ever build)
dist/
build/
